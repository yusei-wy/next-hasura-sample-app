query getArticle($id: uuid!) {
    articles_by_pk(id: $id) {
        subject
        user {
            displayID
            displayName
        }
        createdAt
        updatedAt
        publishedAt
        content
    }
}
